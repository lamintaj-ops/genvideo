# 🎬 Aquaverse Video Generator - Web Application

เว็บแอปพลิเคชันสำหรับสร้างวิดีโอโปรโมชั่น Aquaverse อัตโนมัติ

## 🚀 วิธีการใช้งาน

### 1. ติดตั้ง Dependencies

```bash
pip install flask pandas requests tqdm
```

หรือ

```bash
pip install -r requirements.txt
```

### 2. เริ่มต้นเซิร์ฟเวอร์

```bash
python web_app.py
```

### 3. เปิดเว็บเบราว์เซอร์

ไปที่: `http://localhost:5000`

## 📝 คุณสมบัติ

- ✅ กรอก Prompt เพื่อสร้างวิดีโอ
- ✅ แสดงสถานะการสร้างแบบ Real-time
- ✅ ดาวน์โหลดวิดีโอทั้ง 2 รูปแบบ (16:9 และ 9:16)
- ✅ ประวัติการสร้างวิดีโอ
- ✅ รองรับการทำงานหลายคนพร้อมกัน
- ✅ ลบไฟล์เก่าอัตโนมัติ (หลัง 24 ชม.)

## 🎯 ตัวอย่าง Prompt

```
Family fun waterpark, slides and pools, happy vibes, 15 seconds
```

```
Jumanji adventure zone, exciting water slides, energetic music, 20 seconds
```

```
เล่นน้ำสนุก สไลด์เด็ก สระน้ำครอบครัว บรรยากาศสดใส 15 วินาที
```

## 📁 โครงสร้างไฟล์

```
videoscore/
├── web_app.py              # Flask server
├── templates/
│   └── index.html          # หน้าเว็บหลัก
├── output/
│   ├── jobs/               # ไฟล์วิดีโอที่สร้าง (แยกตาม job_id)
│   ├── final_169.mp4       # วิดีโอ 16:9 ล่าสุด
│   └── final_916.mp4       # วิดีโอ 9:16 ล่าสุด
└── ...
```

## 🔧 API Endpoints

- `GET /` - หน้าเว็บหลัก
- `POST /api/generate` - สร้างวิดีโอใหม่
- `GET /api/status/<job_id>` - เช็คสถานะงาน
- `GET /api/download/<job_id>/<aspect>` - ดาวน์โหลดวิดีโอ
- `GET /api/jobs` - ดูรายการงานล่าสุด

## ⚙️ Configuration

แก้ไขใน `web_app.py`:

```python
app.config['MAX_CONTENT_LENGTH'] = 16 * 1024 * 1024  # ขนาดไฟล์สูงสุด
```

## 🛠️ Troubleshooting

### ปัญหา: ไม่พบ FFmpeg
ตรวจสอบว่า FFmpeg ติดตั้งและอยู่ใน PATH แล้ว:
```bash
ffmpeg -version
```

### ปัญหา: Port 5000 ถูกใช้งาน
เปลี่ยน port ใน `web_app.py`:
```python
app.run(port=8000)  # เปลี่ยนเป็น port อื่น
```

## 📦 Production Deployment

สำหรับ Production ควรใช้:
- **Web Server**: Gunicorn หรือ uWSGI
- **Database**: SQLite/PostgreSQL แทน in-memory dict
- **Queue**: Celery + Redis สำหรับ background tasks
- **Storage**: Cloud storage (S3, GCS) สำหรับเก็บวิดีโอ

ตัวอย่างการรันด้วย Gunicorn:
```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 web_app:app
```

## 📄 License

MIT License
